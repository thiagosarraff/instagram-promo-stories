# ====================================================================
# INSTAGRAM STORIES AUTOMATION - ENVIRONMENT CONFIGURATION
# ====================================================================
# Copy this file to .env and fill in your configuration.
# IMPORTANT: .env file is in .gitignore and should NEVER be committed!
# ====================================================================

# -------- REQUIRED SETTINGS --------
# These variables MUST be set for the application to start

# Instagram account username for posting stories
INSTAGRAM_USERNAME=your_instagram_username

# Instagram account password or app-specific password
# If 2FA is enabled, use an app-specific password instead
INSTAGRAM_PASSWORD=your_instagram_password

# -------- API SERVER CONFIGURATION --------
# Optional - API server will use defaults if not specified

# Port for the API server (default: 5000)
# Must be between 1024 and 65535
API_PORT=5000

# Host binding for the API server (default: 0.0.0.0)
# Use 0.0.0.0 to allow connections from any network interface
# Use 127.0.0.1 for local development only
API_HOST=0.0.0.0

# -------- TEMPLATE CONFIGURATION --------

# Path to story templates directory (default: /app/templates)
# In Docker, this should be the container path
# In local development, this should match your local templates directory
TEMPLATE_PATH=/app/templates

# -------- LOGGING CONFIGURATION --------

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL (default: INFO)
# Use DEBUG for verbose logging, INFO for normal, WARNING/ERROR for production
LOG_LEVEL=INFO

# -------- ADVANCED SETTINGS --------
# Optional - Advanced configuration for performance tuning

# HTTP request timeout in seconds (default: 120, range: 10-600)
REQUEST_TIMEOUT=120

# Path for Instagram session storage (default: /app/sessions)
# Used to maintain Instagram sessions state between requests
INSTAGRAM_SESSIONS_PATH=/app/sessions

# Maximum concurrent story posts (default: 5, range: 1-20)
# Increase for higher throughput, decrease for stability
MAX_CONCURRENT_REQUESTS=5

# ====================================================================
# SECURITY NOTES
# ====================================================================
# - NEVER commit the .env file with real credentials
# - Use Instagram app-specific passwords if 2FA is enabled
# - Keep INSTAGRAM_PASSWORD secure and rotate periodically
# - Review logs to ensure passwords are never exposed
# ====================================================================
